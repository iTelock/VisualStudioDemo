// lc_638_shopping_offers.h: 标准系统包含文件的包含文件
// 或项目特定的包含文件。

#pragma once

#include <iostream>
#include <vector>

class Solution {
public:
    int shoppingOffers(std::vector<int>& price, std::vector<std::vector<int>>& special, std::vector<int>& needs);
};

// TODO: 在此处引用程序需要的其他标头。
