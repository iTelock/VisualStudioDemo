// longest_palindromic_subsequence.h: 标准系统包含文件的包含文件
// 或项目特定的包含文件。

#pragma once

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

class Solution {
public:
    int longestPalindromeSubseq(std::string s);
    int longestPalindromeSubseqPlus(std::string s);
};

// TODO: 在此处引用程序需要的其他标头。
