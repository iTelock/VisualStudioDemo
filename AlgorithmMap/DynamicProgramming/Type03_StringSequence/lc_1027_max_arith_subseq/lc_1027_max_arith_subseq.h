// lc_1027_max_arith_subseq.h: 标准系统包含文件的包含文件
// 或项目特定的包含文件。

#pragma once

#include <iostream>
#include <vector>
#include <unordered_map>
#include <algorithm>

class Solution {
public:
    int longestArithSeqLength(std::vector<int>& nums);
    int longestArithSeqLengthPlus(std::vector<int>& nums);
};

// TODO: 在此处引用程序需要的其他标头。
