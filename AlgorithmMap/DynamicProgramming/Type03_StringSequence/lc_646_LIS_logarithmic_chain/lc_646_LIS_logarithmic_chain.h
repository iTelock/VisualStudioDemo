// lc_646_LIS_logarithmic_chain.h: 标准系统包含文件的包含文件
// 或项目特定的包含文件。

#pragma once

#include <iostream>
#include <vector>
#include <algorithm>
#include <chrono>

class Solution {
public:
    int findLongestChain(std::vector<std::vector<int>>& pairs);
    int findLongestChainPlus(std::vector<std::vector<int>>& pairs);
};

// TODO: 在此处引用程序需要的其他标头。
