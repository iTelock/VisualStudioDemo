// lc_300_LIS.h: 标准系统包含文件的包含文件
// 或项目特定的包含文件。

#pragma once

#include <iostream>
#include <vector>
#include <algorithm>

class Solution {
public:
    int lengthOfLIS(std::vector<int>& nums);
    int lengthOfLISPlus(std::vector<int>& nums);
};

// TODO: 在此处引用程序需要的其他标头。
