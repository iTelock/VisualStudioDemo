中等

# 动态规划状态定义
定义 dp[i]表示偷窃到第 i 个房屋时能获得的最大金额
对于每个房屋，我们有两种选择：
偷窃当前房屋：金额 = dp[i-2] + nums[i]（因为不能偷相邻房屋）
不偷当前房屋：金额 = dp[i-1]（保持前一个状态）
# 状态转移方程
dp[i] = max(dp[i-1], dp[i-2] + nums[i])
# 边界条件
只有1个房屋：dp[0] = nums[0]
有2个房屋：dp[1] = max(nums[0], nums[1])