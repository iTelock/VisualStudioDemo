// largest_square.h: 标准系统包含文件的包含文件
// 或项目特定的包含文件。

#pragma once

#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

class Solution {
public:
    int maximalSquare(std::vector<std::vector<char>>& matrix);
    int maximalSquarePlus(std::vector<std::vector<char>>& matrix);
};

// TODO: 在此处引用程序需要的其他标头。
