// different_paths_v2.h: 标准系统包含文件的包含文件
// 或项目特定的包含文件。

#pragma once

#include <iostream>
#include <vector>
#include <algorithm>

class Solution {
public:
    int uniquePathsWithObstacles(std::vector<std::vector<int>>& obstacleGrid);
    int uniquePathsWithObstacles_plus(std::vector<std::vector<int>>& obstacleGrid);
};

// TODO: 在此处引用程序需要的其他标头。
